import { ReactNode } from "react";

export const metadata = {
  title: "Contact",
  description:
    "Reach out to Unity Threads with questions, feedback, or collaboration ideas."
};

export default function ContactLayout({ children }: { children: ReactNode }) {
  return children;
}
