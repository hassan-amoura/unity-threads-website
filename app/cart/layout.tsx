import { ReactNode } from "react";

export const metadata = {
  title: "Cart",
  description:
    "Review your Unity Threads pieces before a gentle, secure checkout."
};

export default function CartLayout({ children }: { children: ReactNode }) {
  return children;
}
