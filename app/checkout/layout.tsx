import { ReactNode } from "react";

export const metadata = {
  title: "Checkout",
  description:
    "A calm, multi-step checkout designed with clarity and accessibility at the center."
};

export default function CheckoutLayout({ children }: { children: ReactNode }) {
  return children;
}
