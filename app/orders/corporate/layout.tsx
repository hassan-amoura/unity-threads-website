import { ReactNode } from "react";

export const metadata = {
  title: "Corporate orders",
  description:
    "Design sensory-aware apparel for your team, event, or organization with Unity Threads."
};

export default function CorporateOrdersLayout({
  children
}: {
  children: ReactNode;
}) {
  return children;
}
