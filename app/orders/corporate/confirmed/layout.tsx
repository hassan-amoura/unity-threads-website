import { ReactNode } from "react";

export const metadata = {
  title: "Corporate order request received",
  description:
    "Confirmation and next steps for your Unity Threads corporate apparel request."
};

export default function CorporateOrderConfirmedLayout({
  children
}: {
  children: ReactNode;
}) {
  return children;
}
