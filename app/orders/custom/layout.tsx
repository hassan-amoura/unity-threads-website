import { ReactNode } from "react";

export const metadata = {
  title: "Custom orders",
  description:
    "Design a custom Unity Threads piece tailored to your sensory needs, story, or community moment."
};

export default function CustomOrdersLayout({
  children
}: {
  children: ReactNode;
}) {
  return children;
}
