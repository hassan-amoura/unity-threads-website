import { ReactNode } from "react";

export const metadata = {
  title: "Bulk orders",
  description:
    "Request bulk Unity Threads pieces for schools, community groups, fundraisers, or events."
};

export default function BulkOrdersLayout({
  children
}: {
  children: ReactNode;
}) {
  return children;
}
