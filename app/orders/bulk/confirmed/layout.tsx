import { ReactNode } from "react";

export const metadata = {
  title: "Bulk order request received",
  description:
    "Confirmation and next steps for your Unity Threads bulk order request."
};

export default function BulkOrderConfirmedLayout({
  children
}: {
  children: ReactNode;
}) {
  return children;
}
